## Auth Check

- Is the API key/token valid?
- Has the token expired?
- Does the token have required scopes?
- Is the request coming from an allowed IP or client?
- Are environment credentials mismatched (prod vs test)?